# 测试用例设计 - 等价类划分法

## 1. 什么是等价类划分法

把输入数据按"程序处理逻辑是否相同"分成若干类（等价类），
从每类中挑选少量代表性数据进行测试，而不是把所有数据都测一遍。

**目的**：用最少用例发现最多 bug。

## 2. 等价类划分法 - 例子：手机号验证

**需求**：
- 手机号必须是 11 位数字
- 不能为空| 测试场景 | 测试数据 | 预期结果 | 等价类类型 |
|---------|----------|----------|------------|
| 正确手机号 | 13812345678 | 发送验证码成功 | 有效等价类 |
| 手机号为空 | 空 | 提示"请输入手机号" | 无效等价类（边界） |
| 手机号少一位 | 1381234567 | 提示"手机号格式错误" | 无效等价类（长度） |
| 手机号多一位 | 138123456789 | 提示"手机号格式错误" | 无效等价类（长度） |
| 非数字字符 | abcdefghijk | 提示"手机号格式错误" | 无效等价类（类型） |

**等价类划分思路**：
- 有效等价类：长度 11 位 + 全是数字
- 无效等价类：长度不是 11 位、含非数字字符、空

## 3. 我用到的知识

- Markdown 表格语法（`|` 分隔列，`---` 分隔表头）
- 测试用例三要素：**场景 + 数据 + 预期结果**
- 把需求拆成等价类的思路
